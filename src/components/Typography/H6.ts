import styled from 'styled-components'

const H6 = styled.h6`
  font-size: 16px;
  line-height: 22px;
  margin: 0;
  font-weight: 600;
  color: ${({ theme }) => theme.color.colorTextPrimary};
`

export default H6
