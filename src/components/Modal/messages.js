import { defineMessages } from 'react-intl'

const baseId = 'uikit.modal'

export const messages = defineMessages({
  closeButton: {
    id: `${baseId}.closeButton`,
    defaultMessage: 'Закрыть',
    description: 'Закрыть',
  },
})
